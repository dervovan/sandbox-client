import { useState } from "react";
import styles from "../index.module.scss";
import { Link, Paper, Typography } from "@mui/material";

const Merge = () => {
  return (
    <section className={styles.merge}></section>
  )
}

export default Merge